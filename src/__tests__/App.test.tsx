import React from 'react';
import App from '../App';
import { shallow } from 'enzyme';

describe('Suit Test - App Galaxy main', () => {
  
  test('should be defined', () => {
    shallow(<App />);
  })
  
})
